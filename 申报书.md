# MoonBit 开源大赛项目申报书

## 项目名称

MoonBit Motion Lab（原 moonbit-easing，重新定位后将同步更名仓库与 Mooncakes 模块）

## 项目简介

MoonBit Motion Lab 是面向动画、可视化和交互程序的**运动曲线编排与质量验证工具包**。项目不再以收录标准缓动函数为目标；标准固定曲线集合与 CSS 预设均不在交付范围内，以避免和 Mooncakes 上已维护的缓动函数库重叠。

开发者可将自定义函数、参数化 spring/back 曲线或自定义三次贝塞尔曲线接入工具包，再通过统一接口完成：曲线采样、数值报告、端点校验、单调性与超调检测、关键帧时间轴编排，以及固定帧率输出。

## 核心创新与差异化

1. **Motion Profile**：对曲线产生确定性的 `CurveReport`，输出最小/最大值、最大速度、峰值速度时间、单调性和端点误差，适合自动化测试与运动调参。
2. **Motion Timeline**：提供与渲染器无关的顺序时间轴，将多个不同区间、不同持续时间和不同曲线的段落编译为固定帧率数据。
3. **参数化运动原语**：提供显式 frequency/damping 的 spring 和显式 overshoot 的 back，而不是固定的命名公式集合。
4. **自定义曲线接入**：支持用户提供任意 `MotionFn` 与自定义三次贝塞尔控制点；不提供 CSS easing 预设别名。

## 交付范围

- 参数化 back/spring 曲线与通用 `MotionFn`
- 自定义三次贝塞尔求值
- 采样、插值、速度估计、超调和单调性检查
- `CurveReport` 数值质量报告
- `MotionTimeline` 时间轴和固定帧率输出
- 可运行示例、自动化测试、格式/检查/测试/构建 CI

## 明确不做

- 30+ 标准 easing 公式目录
- CSS `ease`、`ease-in`、`ease-out`、`ease-in-out` 预设
- DOM、渲染、动画循环或调度器

## 重新申报说明

旧项目因“标准缓动函数集合”与已有维护库重叠而被驳回。本次修改后，重复的固定公式与 CSS 预设已从源码和文档中移除；项目主交付改为运动曲线的编排、采样与质量验证能力。
