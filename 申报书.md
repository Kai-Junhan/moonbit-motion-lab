# MoonBit 开源大赛项目申报书

- **项目名称**：MoonBit Motion Lab
- **GitHub 仓库**：https://github.com/Kai-Junhan/moonbit-motion-lab
- **项目方向**：动画与可视化基础设施 / 数值质量验证工具
- **项目性质**：原创 MoonBit 项目，不是移植项目

## 项目简介与生态定位

动画、数据可视化和交互程序并不只需要“选一条缓动公式”。在同一条曲线被接入不同渲染器、帧率或数据范围后，开发者还需要确认它是否按时到达端点、是否出现不允许的回退或超调、峰值速度是否超过交互规范，并把结果稳定地交给测试或渲染层。MoonBit 目前已有 `Zlj6566/moonbit-easing` 这类曲线提供者；重复实现 `ease_in_*`、CSS 预设或 bounce/elastic 公式没有生态价值。

MoonBit Motion Lab 的输入是调用方已经选定的 `MotionFn`，包括可由 `Zlj6566/moonbit-easing` 提供的函数；交付是曲线的数值报告、验收结论和固定帧率时间轴数据。它不实现或命名任何缓动公式，因此与已有包是串联关系：已有包解决“曲线是什么”，本项目解决“这条曲线是否满足要求，以及如何生成可复现的数值过程”。这使其可同时服务 MoonBit 的 UI、游戏、数据可视化、测试工具和离线数据处理，而不绑定具体渲染器。

## 预期使用场景

1. **交互控件验收**：设计或业务代码选定一条外部 easing 函数后，以端点误差、禁止回退、最大超调和最大速度作为策略检查；不合格的曲线在进入 UI 前被测试拦截。
2. **可视化过渡预计算**：图表工具把任意数值曲线交给 Motion Lab，按指定帧率生成确定的坐标数组；Canvas、WebGL 或原生渲染器只消费数组，减少各端重复采样造成的差异。
3. **视觉回归与黄金数据**：测试为同一曲线和时间轴生成稳定帧值与 `CurveReport`，把端点、单调性、超调和峰值速度纳入断言，无需依赖浏览器计时或动画运行时。

## 核心交付与工程边界

- `MotionFn` 曲线接入接口，以及确定性采样、插值、速度、单调性和超调计算；
- `CurveReport`：最小/最大值、最大速度、峰值速度时间、单调性和端点误差；
- `CurvePolicy`/`check`：以显式容差、是否单调、允许超调和最大速度形成可断言的验收结果；
- `MotionTimeline`：将多个数值区间和调用方曲线编译为固定帧率数组；
- 三个可运行场景、单元测试与 GitHub Actions，验证数值结果和边界输入。

明确不做：标准或命名 easing 函数目录、CSS `ease*` 预设、spring/back/bounce/elastic/cubic-Bezier 公式实现、DOM/渲染器、动画调度器或运行时。本项目不复制 `Zlj6566/moonbit-easing` 的 API 或源码；使用者可自行组合两个包。

## 实现路径与原创说明

项目采用纯 MoonBit 的确定性离散采样：以固定样本数计算差分速度和范围指标，再以策略阈值给出可测试的验收结论；时间轴仅做数值段落拼接和固定频率导出，不承担渲染或调度。这样避免了不同运行时计时、帧率和渲染 API 对结果的影响。实现需要函数类型、浮点数值分析、边界条件和可复现测试能力，所有核心代码将以 MoonBit 原生包、类型和测试组织。

项目为原创实现，不移植或改写第三方项目，因此无移植来源和上游许可证需要声明；本仓库以 Apache-2.0 发布。`Zlj6566/moonbit-easing` 仅作为已存在生态包的兼容对象和边界参照：https://github.com/Zlj6566/moonbit-easing。
