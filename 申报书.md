# moonbit-easing 项目申报书

## 基本信息

- **项目名称**：moonbit-easing - MoonBit 缓动函数库
- **参赛者**：开俊涵
- **联系方式**：[待填写]
- **GitHub 仓库链接**：https://github.com/Kai-Junhan/moonbit-easing
- **Gitlink 仓库链接**：[待填写]
- **项目方向**：动画/图形工具库
- **是否为移植项目**：否，原创项目

---

## 项目简介

moonbit-easing 是一个全面的缓动函数（easing functions）库，为 MoonBit 生态提供 30+ 经典缓动曲线、三次贝塞尔曲线支持和动画工具。项目填补了 MoonBit 在动画和过渡效果方面的基础设施空白。

**核心痛点**：动画和可视化应用需要流畅的过渡效果，而非线性的突变。缓动函数提供数学化的加速/减速曲线，让动画看起来自然、专业。目前 MoonBit 生态缺少这一基础能力，开发者只能使用简单线性插值或手写复杂的数学公式。

**生态定位**：作为动画和交互的基础库，moonbit-easing 可以被 UI 框架、游戏引擎、数据可视化工具依赖，提供标准化的动画曲线。与 moonbit-color 结合可实现平滑的色彩过渡动画。

---

## 项目方向与适用场景

### 应用场景

1. **UI 动画过渡**
   - 按钮点击、对话框弹出、页面切换
   - 菜单展开/收起、标签页切换
   - 加载动画、进度条填充

2. **数据可视化动画**
   - 图表更新时数据点平滑移动
   - 柱状图高度过渡、饼图角度变化
   - 地图缩放、时间轴播放

3. **游戏与交互效果**
   - 角色跳跃、相机运动、道具掉落
   - 粒子系统、物理模拟
   - UI 反馈效果（弹簧、弹跳）

4. **生成艺术**
   - 程序化动画创作
   - 基于时间的参数变化
   - 音乐可视化

### 技术价值

- **填补生态空白**：MoonBit 首个专业缓动函数库
- **标准化**：遵循 CSS Easing 和 Robert Penner 标准
- **可组合**：函数式设计支持缓动自由组合
- **高精度**：贝塞尔曲线使用牛顿-拉弗森法求解

---

## 拟实现的核心功能

### 1. 经典缓动函数（30+）

基于 **Robert Penner's Easing Functions** 标准实现，涵盖所有常用缓动类型：

| 类型 | 函数 | 特性 | 应用场景 |
|------|------|------|----------|
| **Linear** | linear | 匀速运动 | 简单过渡 |
| **Quadratic** | ease-in/out/in-out-quad | 二次方加速 | 温和动画 |
| **Cubic** | ease-in/out/in-out-cubic | 三次方加速 | 标准动画 |
| **Quartic** | ease-in/out/in-out-quart | 四次方加速 | 快速动画 |
| **Quintic** | ease-in/out/in-out-quint | 五次方加速 | 极速动画 |
| **Sine** | ease-in/out/in-out-sine | 正弦曲线 | 自然过渡 |
| **Exponential** | ease-in/out/in-out-expo | 指数增长 | 缩放动画 |
| **Circular** | ease-in/out/in-out-circ | 圆形曲线 | 圆周运动 |
| **Back** | ease-in/out/in-out-back | 超出后拉回 | 弹性效果 |
| **Elastic** | ease-in/out/in-out-elastic | 弹簧振荡 | 弹性反馈 |
| **Bounce** | ease-in/out/in-out-bounce | 弹跳落地 | 球体物理 |

**缓动分类**：
- **ease-in**：慢启动，逐渐加速（适合离开屏幕）
- **ease-out**：快启动，逐渐减速（适合进入屏幕）
- **ease-in-out**：慢启动 + 慢结束（适合往返动画）

### 2. 三次贝塞尔曲线

**核心能力**：
- 自定义贝塞尔曲线（4 个控制点 P0-P3）
- 牛顿-拉弗森法数值求解（给定 X 求 Y）
- 10 次迭代，精度 0.0001

**CSS cubic-bezier 预设**：
- `ease` - cubic-bezier(0.25, 0.1, 0.25, 1.0)
- `ease-in` - cubic-bezier(0.42, 0, 1.0, 1.0)
- `ease-out` - cubic-bezier(0, 0, 0.58, 1.0)
- `ease-in-out` - cubic-bezier(0.42, 0, 0.58, 1.0)

**技术细节**：
```moonbit
// 牛顿迭代法求解 X(t) = x
fn solve_bezier_x(x: Double, x1: Double, x2: Double) -> Double {
  let mut t = x  // 初始猜测
  for i = 0; i < 10; i = i + 1 {
    let xt = bezier_x(t, x1, x2)
    let slope = bezier_x_derivative(t, x1, x2)
    t = t - (xt - x) / slope
  }
  t
}
```

### 3. 缓动工具函数

**插值与序列**：
- `ease_value(start, end, t, easing)` - 在起始值和结束值间应用缓动
- `ease_sequence(start, end, steps, easing)` - 生成 N 帧动画数组

**缓动变换**：
- `chain_easing(first, second, split)` - 前半段用 A，后半段用 B
- `reverse_easing(easing)` - ease-in 变 ease-out
- `mirror_easing(easing)` - 生成 ease-in-out 效果
- `clamp_easing(easing)` - 限制输出在 [0, 1] 区间

**示例**：
```moonbit
// 生成60帧动画（1秒@60fps）
let frames = @easing.ease_sequence(0.0, 100.0, 60, @easing.ease_out_cubic)

// 自定义组合：前半段快速启动，后半段弹跳结束
let custom = @easing.chain_easing(
  @easing.ease_out_cubic,
  @easing.ease_out_bounce,
  0.5
)
```

---

## 参考资料与开源规范

### 标准与理论依据

- **Robert Penner's Easing Functions** - 缓动函数行业标准
  - http://robertpenner.com/easing/
- **easings.net** (CC BY-SA 4.0) - 缓动曲线可视化参考
  - https://easings.net
- **W3C CSS Easing Functions Level 1** - cubic-bezier 标准规范
  - https://www.w3.org/TR/css-easing-1/
- **Newton-Raphson Method** - 数值求解算法
  - https://en.wikipedia.org/wiki/Newton%27s_method

### 参考项目（思路参考）

- **easing-utils** - 各语言通用公式参考
- **bezier-easing** (MIT) - JavaScript 贝塞尔实现思路
  - https://github.com/gre/bezier-easing

**声明**：所有代码从零实现，仅参考数学公式和算法原理，不复制任何现有项目代码。算法基于公开的数学公式（Robert Penner、Newton-Raphson）独立编写。

### 本项目许可证

**Apache License 2.0** - 允许商业使用、修改和分发，要求保留版权声明。

---

## 计划开发内容

### ✅ 已完成内容

#### 核心功能实现
- ✅ 30+ 经典缓动函数（linear, quad, cubic, quart, quint, sine, expo, circ, back, elastic, bounce）
- ✅ 三次贝塞尔曲线实现（牛顿法求解）
- ✅ CSS cubic-bezier 预设（ease 系列）
- ✅ 5 个工具函数（插值、序列、链接、反转、镜像）
- ✅ 类型安全的 API 设计（`EasingFn = (Double) -> Double`）

#### 测试覆盖
- ✅ 25 个单元测试
- ✅ 覆盖所有缓动函数和工具
- ✅ 边界条件测试（t=0, t=1）
- ✅ 贝塞尔精度测试

#### 工程化
- ✅ GitHub Actions CI/CD（check/test/build）
- ✅ 完整的 README.md 文档（中英双语）
- ✅ API 参考和使用示例
- ✅ 项目申报书（PROPOSAL.md）
- ✅ LICENSE（Apache 2.0）
- ✅ .gitignore 和 moon.mod.json 配置

#### 示例程序
- ✅ examples/basic/ 基础使用示例
- ✅ 覆盖所有核心 API

---

## 技术路径

### 1. 经典缓动函数实现

**基于 Robert Penner 公式**，每个函数都处理边界值：

**二次方缓动**：
```moonbit
fn ease_in_quad(t: Double) -> Double {
  t * t
}

fn ease_out_quad(t: Double) -> Double {
  t * (2.0 - t)
}

fn ease_in_out_quad(t: Double) -> Double {
  if t < 0.5 {
    2.0 * t * t
  } else {
    -1.0 + (4.0 - 2.0 * t) * t
  }
}
```

**弹性缓动**（正弦 + 指数衰减）：
```moonbit
fn ease_out_elastic(t: Double) -> Double {
  if t == 0.0 || t == 1.0 { return t }
  let p = 0.3
  pow(2.0, -10.0 * t) * sin((t - p / 4.0) * (2.0 * pi) / p) + 1.0
}
```

**弹跳缓动**（分段多项式）：
```moonbit
fn ease_out_bounce(t: Double) -> Double {
  if t < 1.0 / 2.75 {
    7.5625 * t * t
  } else if t < 2.0 / 2.75 {
    let t2 = t - 1.5 / 2.75
    7.5625 * t2 * t2 + 0.75
  } else if t < 2.5 / 2.75 {
    let t2 = t - 2.25 / 2.75
    7.5625 * t2 * t2 + 0.9375
  } else {
    let t2 = t - 2.625 / 2.75
    7.5625 * t2 * t2 + 0.984375
  }
}
```

### 2. 三次贝塞尔曲线

**核心原理**：
- 三次贝塞尔曲线参数方程：
  - `X(t) = 3(1-t)²t·x1 + 3(1-t)t²·x2 + t³`
  - `Y(t) = 3(1-t)²t·y1 + 3(1-t)t²·y2 + t³`
- 给定 X，用牛顿法求 t，再计算 Y(t)

**实现步骤**：
1. 定义贝塞尔 X 函数和其导数
2. 牛顿迭代：`t_new = t - f(t) / f'(t)`
3. 迭代 10 次达到 0.0001 精度
4. 用求得的 t 计算 Y 值

**优化**：
- 边界快速返回（t=0 返回 0，t=1 返回 1）
- 初始猜测为 t = x（通常很接近）
- 导数为零时特殊处理

### 3. 缓动组合

**高阶函数设计**：
```moonbit
fn chain_easing(
  first: (Double) -> Double,
  second: (Double) -> Double,
  split: Double
) -> (Double) -> Double {
  fn(t: Double) -> Double {
    if t < split {
      first(t / split) * split
    } else {
      split + second((t - split) / (1.0 - split)) * (1.0 - split)
    }
  }
}

fn reverse_easing(easing: (Double) -> Double) -> (Double) -> Double {
  fn(t: Double) -> Double { 1.0 - easing(1.0 - t) }
}

fn mirror_easing(easing: (Double) -> Double) -> (Double) -> Double {
  fn(t: Double) -> Double {
    if t < 0.5 {
      easing(t * 2.0) / 2.0
    } else {
      1.0 - easing((1.0 - t) * 2.0) / 2.0
    }
  }
}
```

### 4. 插值与序列生成

```moonbit
fn ease_value(
  start: Double,
  end: Double,
  t: Double,
  easing: (Double) -> Double
) -> Double {
  start + (end - start) * easing(t)
}

fn ease_sequence(
  start: Double,
  end: Double,
  steps: Int,
  easing: (Double) -> Double
) -> Array[Double] {
  let result = Array::new()
  for i = 0; i < steps; i = i + 1 {
    let t = i.to_double() / (steps - 1).to_double()
    result.push(ease_value(start, end, t, easing))
  }
  result
}
```

---

## 预期交付成果

### 1. 核心库 ✅
- **moonbit-easing** 包
- 零外部依赖（除 moonbitlang/core）
- 代码规模：约 600 行核心代码 + 400 行测试

### 2. 测试套件 ✅
- 25 个单元测试
- 覆盖所有公开 API
- CI 自动运行，100% 通过

### 3. 文档 ✅
- **README.md** - 项目介绍、快速开始、API 参考、示例（中英双语）
- **PROPOSAL.md** - 项目申报书
- 代码内联注释

### 4. 示例程序 ✅
- examples/basic/ - 基础使用示例
- 覆盖缓动函数、贝塞尔曲线、工具函数

### 5. 工程配置 ✅
- GitHub Actions CI
- .gitignore / LICENSE / moon.mod.json
- 代码格式化规范（moon fmt）

### 6. 发布 📅
- 发布到 mooncakes.io（验收前完成）

---

## 项目现有基础

截至报名提交时，本项目已完成：

1. **核心功能 100%**
   - 30+ 经典缓动函数全部实现
   - 贝塞尔曲线和 CSS 预设
   - 所有工具函数
   - API 设计清晰，类型安全

2. **测试覆盖充分**
   - 25 个单元测试全部通过
   - 边界条件和精度测试

3. **文档完整**
   - 中英双语 README
   - API 参考和使用示例
   - 项目申报书

4. **工程质量**
   - GitHub Actions CI 配置
   - 代码格式化和类型检查通过
   - 项目结构清晰

5. **开源合规**
   - Apache 2.0 许可证
   - 代码独立实现，遵守参考资料许可

---

## 项目价值与创新点

### 生态价值
- **填补空白**：MoonBit 首个专业缓动函数库
- **基础设施**：为动画、游戏、可视化提供底层支持
- **标准化**：遵循 CSS 和行业标准，降低学习成本

### 技术亮点
- **全面性**：30+ 缓动函数涵盖所有常见需求
- **精确性**：贝塞尔曲线数值求解精度可控
- **组合性**：函数式设计支持自由组合
- **零依赖**：纯 MoonBit 实现，易于集成

### 可扩展性
- **缓动函数可扩展**：用户可定义自己的缓动曲线
- **算法可组合**：chain/reverse/mirror 支持复杂动画
- **平台无关**：纯计算库，可编译为任何后端

### 协同效应
与 **moonbit-color** 结合实现流畅色彩动画：
```moonbit
// 红到蓝的缓动渐变
let frames = @easing.ease_sequence(0.0, 1.0, 60, @easing.ease_out_cubic)
for t in frames {
  let color = @color.RGB::lerp(red, blue, t)
  render(color)
}
```

---

## 工作量统计

- **核心代码**：~600 行 MoonBit（easing.mbt + bezier.mbt + elastic_bounce.mbt + utils.mbt）
- **测试代码**：~400 行 MoonBit（easing_test.mbt）
- **文档**：~600 行 Markdown（README.md + PROPOSAL.md + 申报书）
- **配置文件**：GitHub Actions、moon.mod.json、.gitignore 等

**总计**：约 1,600 行有效代码与文档，符合比赛 4,000~10,000 行参考区间的入门级项目。

---

## 明确不做的内容

- 不实现动画时间轴管理（属于动画引擎范畴）
- 不提供 DOM 操作或渲染功能（仅提供数学计算）
- 不实现关键帧系统（缓动函数是单一曲线）
- 不处理并发动画调度（属于运行时范畴）

---

## 后续维护计划

1. **功能扩展**
   - 更多缓动预设（Material Design、iOS 动画曲线）
   - 高阶贝塞尔曲线（4 次、5 次）
   - 路径跟随动画（沿 SVG 路径运动）

2. **性能优化**
   - 贝塞尔曲线查找表缓存
   - SIMD 加速批量计算

3. **生态集成**
   - 与 MoonBit UI 框架协作
   - 为游戏引擎提供动画支持

4. **文档完善**
   - 撰写动画设计教程
   - 添加交互式可视化示例

---

## 联系方式

- **参赛者**：开俊涵
- **GitHub**：https://github.com/Kai-Junhan
- **项目仓库**：https://github.com/Kai-Junhan/moonbit-easing
- **邮箱**：[待填写]
- **微信**：[待填写]

---

**声明**：本项目所有代码从零独立实现，仅参考公开的数学公式和算法原理（Robert Penner、Newton-Raphson、CSS 标准）。不包含任何第三方项目的代码拷贝。
